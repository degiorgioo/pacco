const clc = require("cli-color");

/**
 * Colors.
 */
const pink = clc.xterm(204);
const yellow = clc.xterm(227);
const blue = clc.xterm(39);
const red = clc.xterm(9);
const green = clc.xterm(30);
const orange = clc.xterm(215);

module.exports = {
  pink,
  yellow,
  blue,
  red,
  green,
  orange,
};
